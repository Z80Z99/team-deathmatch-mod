# 地图道具

## r115 编辑入口合并与观战点优化

- 地图工作台的“编辑地图”、新建地图后的“去编辑这张地图”、手持规划器右键和画笔菜单的规划器入口，现在统一打开地图规划器。
- 独立旧地图编辑页已移除；区域范围统一由“规划器选择目标 + 画笔即时保存”完成，不再保留另一套脚下取角、保存草稿流程。
- 规划器继承旧编辑页的服务器状态同步：打开时请求最新状态，菜单中可手动“刷新状态”，并每秒后台轮询。
- 出生点备用操作按“当前目标”合并：先在循环按钮选择 A/B/C/D 队或观战目标，再使用“脚下添加/设置”或“清空当前目标”；清空会先确认。
- 观战出生点允许放在地图边界外、高处或空中，只要求与地图同维度；加载和修改地图区域时不会误删地图外观战点。

## r113 地图编辑界面重设计

- 地图编辑页改为“目标状态 → 地图摘要 → 工具工作流 → 当前任务”的顺序；页签切换只保留当前任务行，小分辨率下不再预留大片空行。
- 地图编辑页的规划器、画笔、获得工具和教程整合为一行主工作流；脚下取角、出生点清空等旧操作保留为“备用精确操作”，每个功能旁都有 `？` 说明。
- 规划器菜单顶部显示地图、当前目标、区域数量和缺失必需区域，并用彩色提示直接给出“下一步”。
- 规划器区域按“基础区域 / 玩法区域 / 自定义与其他 / 出生点工具”分组；每个分组与区域都有 `？` 说明，悬停区域名称仍会让边框发光。
- 画笔菜单顶部显示地图、当前目标、模式和空气距离；目标、模式和距离变化后立即更新，并用“下一步”提示当前应框选、逐格增删还是添加出生点。
- 画笔菜单保留“交互说明”“完整教程”、规划器入口和地图工作台入口；所有设置仍自动保存。

## r112 完整教程与表达式指南

游戏内新增“地图工具教程”，入口覆盖地图工作台、地图编辑页、规划器、画笔和区域属性页。教程支持章节切换、滚轮阅读、自动换行和小分辨率布局检查，并包含“表达式语法”与“示例集”专章。

### 快速开始

1. 按 `K` 打开地图工作台，或从暂停菜单进入“我的地图”。
2. 点击“获得工具”，领取地图规划器和地图画笔。
3. 新建地图并进入编辑；手持规划器右键打开菜单。
4. 创建地图边界、重置区域和 A/B 队复活区域。
5. 用画笔圈定范围，必要时进入区域属性页设置显示与出现逻辑。
6. 分别用团队竞技、爆破模式测试，确认出生点、边界和地图恢复正常。
7. 生成邀请码并复制给其他玩家导入副本。

### 必需区域

- 地图边界：比赛活动空间；越界会触发警告、倒计时和判罚，也影响方块保护范围。
- 重置区域：赛后恢复范围；与边界一致时可选择“同时作为重置范围”，也可使用更小的独立范围。
- 出生点：固定出生策略至少需要 A/B 队；C/D 队仅在三队、四队房间需要。随机出生不要求预设出生点，但不能替代边界和重置区域。
- 推荐使用 `A队复活区域`、`B队复活区域`、`C队复活区域`、`D队复活区域`。存在可用复活区域时优先使用区域，否则回退旧出生点标记。

### 规划器

- 手持规划器单按右键打开菜单；准星指向区域时边框呼吸发光。
- 左键选定准星指向的区域；站在区域 A 内指向区域 B 时优先选择 B。
- 区域重叠或交错时，右键菜单列出全部可选项；悬停名称会让对应区域发光。
- 菜单顶部可切换第一人称、背后观察、正面观察；“继续编辑”保留视角，“恢复进入前视角”还原视角。
- 可创建地图边界、重置区域和自定义区域；自定义区域创建后自动成为画笔目标。

### 画笔

- 必须先在规划器中创建或选定目标；手持画笔蹲下右键打开画笔菜单。
- 两角框选：左键端点 A，右键端点 B；两个角可为方块或空气，确认后自动保存。
- 逐格增删：左键移除一格，右键在相邻位置添加一格；支持空洞和不连续区域。
- 右键可选择空气，默认取前方 2 格；前方有方块阻挡时优先命中方块。空气距离可在 1～64 格间调整。
- 画笔只修改地图区域配置，不会挖掘或放置真实方块。
- 手持工具时左下角显示操作提示，右下角显示目标坐标等信息；受影响目标显示玻璃式指示标识。

### 区域属性

- 名称、类型、备注：用于识别和记录设计意图。
- 对局内显示、显示范围、外观策略：控制对局提示与区域视觉。
- 颜色、边框、填充：控制区域显示效果。
- 优先级：区域重叠时数字越大越优先。
- 出现逻辑与模式 / 条件：决定区域何时参与玩法；复活区域当前已实时生效。

### 出现逻辑

- `始终生效`：不检查比赛状态。
- `仅比赛中`：热身和正式比赛阶段生效。
- `指定模式`：只支持一个模式；多模式请使用条件表达式。
- `条件表达式`：支持 `match / match_active`、`mode`、`round`、`teams / team_count`、`players / player_count`，运算符 `== != < <= > >= ! && ||`，以及括号和 `true / false`。

团队竞技与爆破模式共用出生区域：

```text
mode == "TEAM_DEATHMATCH" || mode == "SEARCH_DESTROY"
```

等价写法：

```text
mode == "TDM" || mode == "SD"
mode == "团队竞技" || mode == "爆破模式"
```

模式别名：

```text
TEAM_DEATHMATCH / TDM / 团队竞技
SEARCH_DESTROY / SD / bomb / 爆破模式
LAST_STANDING / 歼灭竞技
```

注意：`capture`、`hotspot` 是区域类型，不是已实现比赛模式。表达式为空或解析失败时区域不生效。

### 表达式语法

一行表达式可以按“变量 → 比较符 → 逻辑词”理解。例如：

```text
match && mode == "SEARCH_DESTROY" && round == 1
```

含义是：当前处于比赛，并且模式是爆破模式，并且回合数是 1。

- 变量：`match / match_active`、`mode`、`round`、`teams / team_count`、`players / player_count`。
- 布尔值：`true / false`；数字：`1`、`2`、`8`；模式文本：`"SEARCH_DESTROY"`。
- 比较符：`==`、`!=`、`<`、`<=`、`>`、`>=`。
- 逻辑词：`!`、`&&`、`||`，支持括号分组。
- 优先级：`!` → 比较符 → `&&` → `||`。

常用组合：

```text
match
mode == "SEARCH_DESTROY"
round == 1 || round == 2
teams >= 3 && players >= 8
match && (mode == "SD" || mode == "LAST_STANDING")
```

### 初阶到高阶示例

多模式共用出生区域：

```text
mode == "TEAM_DEATHMATCH" || mode == "SEARCH_DESTROY"
```

爆破模式第 1 回合使用 A 爆破点：

```text
match && mode == "SEARCH_DESTROY" && round == 1
```

爆破模式第 2 回合使用 B 爆破点：

```text
match && mode == "SEARCH_DESTROY" && round == 2
```

第 3 回合及以后使用 C 爆破点：

```text
match && mode == "SEARCH_DESTROY" && round >= 3
```

团队竞技任意回合，或爆破模式第 2 回合之后：

```text
match && (mode == "TEAM_DEATHMATCH" || (mode == "SEARCH_DESTROY" && round > 1))
```

当前限制：表达式不支持算术和取余，因此不能写 `round % 2 == 1` 来自动切换奇偶回合；需要为具体回合或回合范围分别创建区域。

### 测试与分享

- 基础检查：地图状态不为未完成，边界与重置区域已保存，快照就绪。
- 模式测试：团队竞技测试复活；爆破模式测试开局、回合与换边；三队、四队房间测试 C/D 队出生。
- 边界与恢复：测试越界倒计时、返回逻辑、赛后地图恢复。
- 分享：生成邀请码、复制邀请码、撤销邀请码；导入会生成独立副本，删除地图不会同步删除已导入副本。

## r110 区域激活逻辑

- 自定义出生区域现在会按规划器中的“出现逻辑”参与比赛：`始终生效`、`仅比赛中`、`指定模式`、`条件表达式` 均已在热身和正式比赛阶段生效。
- `指定模式` 支持英文模式名、中文模式名和常用缩写：`TEAM_DEATHMATCH / TDM / 团队竞技`，`SEARCH_DESTROY / SD / bomb / 爆破模式`，`LAST_STANDING / 歼灭竞技`。
- `条件表达式` 支持 `match`、`mode`、`round`、`teams`、`players` 变量，`true/false`、括号、`!`、`&&`、`||`，以及 `==`、`!=`、`<`、`<=`、`>`、`>=`。表达式为空或无法解析时区域不生效。
- 激活状态变化会清空固定出生点缓存，避免上一场比赛的筛选结果影响下一场。

## r90 边界规则与逐格编辑

- 正式比赛启用区域限制时，玩家越界后有 10 秒返回时间，显示逐渐加深的暗红遮罩和倒计时；返回立即解除。超时计阵亡，遵循房间复活/歼灭规则，不直接强制传回。
- 比赛期间地图维度内、边界外方块禁止修改。普通挖掘、爆炸和 Level 方块写入受保护；Explosion Overhaul 0.2.3.0 的直接区块写入有独立兼容拦截。地图重置的内部写入允许恢复独立重置区。
- 两角模式左右键均支持空气取点。逐格模式左键移除一格、右键在区域相邻位置添加一格，允许内部空洞与不连续区域；不是修改实际地形。最后一格移除后删除区域，画笔需重新选目标。
- 服务端确认保存后播放 0.45 秒过渡：移除为半透明红色，添加为半透明青色，常驻区域仍为线框。
- 区域形状按长方体片段保存和同步，单区最多 1024 片段；快照按包围盒限制内存，恢复时跳过空洞，并验证形状摘要防止旧快照误用。
- HUD：组件列表选择不跳转；HUD 示例默认折叠，取消状态文字示例入口；数据来源按场景过滤；参数帮助提供输入/显示对照。
- HUD 条件增加复活中、可作战、比赛中、热身中、越界、观战和其他组件隐藏后；编辑器不因条件隐藏元素。第三方可用 HudApi.registerCondition("api:id", supplier) 注册条件。
- HUD 动画可选无、淡入淡出、滑入滑出、缩放渐变，时长 50～2000ms；复活进度使用同步总时长，其他没有自然上限的数据可手动设置上限，时间类上限按秒填写。
- r90 网络协议为 15，客户端与服务器需同时更新。爆炸模组的未来版本或其他直接写区块的模组需要重新验证；复活后发生的新爆炸仍可破坏地图内地形。

## r89 动态随机复活

- 随机模式维护最多 128 个候选点，每 tick 分批扫描 256 个位置；每 20 tick 复查候选点，破坏后移除、修复或开辟的新位置扫描后加入。大地图的完整巡检需要更长时间，不代表每次已扫描全图。
- 比赛阵亡（含拦截致命伤害和原版死亡）请求刷新；随后一次 tick 复查候选并扫描 1024 个位置，同一 tick 多人死亡合并处理。
- 每次真正复活前再次读取实际地形检查候选点；无候选时再进行有上限的随机搜索，仍无安全点则提示并观战，不使用失效坐标。
- 针对 Explosion Overhaul 0.2.3.0 的跨 tick 弹坑破坏，不只依赖爆炸事件或死亡瞬间的地形；不需要硬依赖该模组。复活后的新爆炸仍可能造成伤害或破坏地面。
- 切换地图、非随机模式及回合重置期间清除候选缓存，不写入地图预设出生点文件。

## r88 编辑与随机出生

- 规划器选中地图边界后，可选择“同时作为重置范围”；之后修改边界，重置范围同步跟随。也可切回独立重置区域。
- 房间“地图内安全随机”不要求队伍预设出生点；开赛和每次复活都搜索安全位置。检查 3×3 周围地面支撑、两格空间、碰撞、液体和危险寻路类型，并清除传送前的下落速度。
- 搜索有次数上限；找不到安全位置时阻止开赛，比赛中找不到时暂时转观战并提示，避免危险传送。
- 画笔必须先在规划器中创建或选中区域。更换地图、删除目标后清除选中状态。出生点目标也在规划器选择。
- 自定义区域初始为脚下一个方块，创建后自动成为画笔目标。

## r87 删除修复

- 修复删除区域时误用区域 ID 作为地图 ID 的问题；删除基础区域和自定义区域均写回原地图。
- 不自动删除旧版本可能误生成的地图文件，避免误删用户同名地图。

## 规划器

- 右键打开区域菜单，左键选定准星指向的区域。
- 右键菜单保留当前视角；顶部可直接选择第一人称、背后观察或正面观察，当前选项高亮。
- 菜单会优先显示准星命中的区域；重叠区域会同时列出。站在区域 A 内指向区域 B 时，会优先选择 B。
- 可创建和编辑自定义区域的名称、类型、显示范围、外观策略、出现逻辑、优先级、颜色、边框和填充。
- 每个区域和功能旁都有【？】按钮，可展开该项的详细说明。
- “继续编辑”关闭菜单并保留所选视角；“恢复进入前视角”还原打开菜单前的视角并关闭。
- 地图边界、重置区域、A/B/C/D 出生点和观战出生点仍可通过画笔编辑。

## 画笔

- 先在工作台选择地图，再在画笔菜单选择编辑目标；自定义区域要先用规划器选中。
- 两角框选：左右键各选一个角，支持空气取点；长方体区域完成后自动保存。
- 逐格增删：左键移除一格、右键相邻添加一格，支持内部挖空；已修改形状不会在重置时被补齐。
- 出生点：右键添加、左键移除，不需要切换模式。
- 右键可选择空气，默认距离玩家前方 2 格，可在画笔菜单中调整。
- 画笔不会破坏方块；手持时用纯线框标出目标方块、空气取点和区域，不绘制填充面。
- 蹲下右键打开画笔菜单，可切换区域/方块模式和调整选择距离。
- 画笔菜单的模式和距离设置旁都有【？】按钮，可查看详细说明。
- 模式和距离自动记入当前服务器的玩家存档，重连后恢复；更换区域不会重置模式。
- 手持任一道具时，屏幕左下角显示操作提示，右下角显示目标坐标。

## 工作台整合

- 地图工作台提供“获得工具”，可领取地图规划器和地图画笔。
- 重复领取只补缺失工具；背包、副手及鼠标上已有的工具（包括改名工具）不会重复发放。背包满时会提示腾出空位。
- 地图编辑页提供“打开规划器”“打开画笔”和“获得工具”。
- 规划器菜单与画笔菜单都保留“地图工作台”入口，便于切换地图和分享。
- 编辑页的“脚下”按钮只修改草稿，需要点击“保存区域”；画笔完成框选会自动保存。

## r83 修复

- 修复拿地图工具左键点击空气时取消不可取消事件造成的客户端崩溃。
- 蹲下右键统一为打开画笔菜单，服务端不再暗中切换模式；编辑操作统一由画笔数据包执行。
- 教程文字自动换行并随菜单滚动，手持提示按当前目标与模式显示。

## r84 修复

- 修复地图同步包中 C/D 队人数和区域数据读写顺序不一致，导致区域、选中目标和距离无法正确同步的问题。
- 范围线框与填充分开绘制；第一角确认后显示黄色待完成范围，第二角完成后显示已保存区域。保存失败时保留角点并显示原因。
- 空气距离使用 1～64 格滑块和加减按钮；左键点击的实际方块不再受空气距离限制。
- 左键只处理按下事件，避免松开或持续挖掘重复提交角点。
- 联机协议升级为 13，客户端和服务器都需要更新到 r84。

## r85 显示调整

- 编辑预览统一为纯外框，移除区域、角点及目标方块的半透明填充面，避免大片颜色遮挡场景。
- 保留区域颜色、选中高亮和未完成框选预览；已有地图无需重新创建。

## r86 用户创建基础区域

- 新地图不再自动生成地图边界、重置区域和出生点，初始状态明确标记为“未完成”。
- 地图作者在规划器中自行创建地图边界和重置区域，再用画笔圈定范围。
- 两种基础区域都可以删除；删除后地图回到未完成状态，可随时重新创建。
- 缺少基础区域的地图不能加载或开赛，房间会明确提示缺少的项目。
- 旧地图默认保持原有基础区域，不需要迁移。
