# 地图道具

## 规划器

- 右键打开区域菜单，左键选定准星指向的区域。
- 右键菜单保留当前视角；顶部可直接选择第一人称、背后观察或正面观察，当前选项高亮。
- 菜单会优先显示准星命中的区域；重叠区域会同时列出。站在区域 A 内指向区域 B 时，会优先选择 B。
- 可创建和编辑自定义区域的名称、类型、显示范围、外观策略、出现逻辑、优先级、颜色、边框和填充。
- 每个区域和功能旁都有【？】按钮，可展开该项的详细说明。
- “继续编辑”关闭菜单并保留所选视角；“恢复进入前视角”还原打开菜单前的视角并关闭。
- 地图边界、重置区域、A/B/C/D 出生点和观战出生点仍可通过画笔编辑。

## 画笔

- 先在工作台选择地图，再在画笔菜单选择编辑目标；自定义区域要先用规划器选中。
- 两角框选：左键点击一个角的实际方块，再右键选对角；长方体区域完成后自动保存。
- 调整边缘：右键把区域扩展到目标，左键收缩区域边缘。区域始终为长方体，不能挖掉内部单块。
- 出生点：右键添加、左键移除，不需要切换模式。
- 右键可选择空气，默认距离玩家前方 2 格，可在画笔菜单中调整。
- 画笔不会破坏方块；手持时显示目标方块/空气的玻璃感指示。
- 蹲下右键打开画笔菜单，可切换区域/方块模式和调整选择距离。
- 画笔菜单的模式和距离设置旁都有【？】按钮，可查看详细说明。
- 模式和距离自动记入当前服务器的玩家存档，重连后恢复；更换区域不会重置模式。
- 手持任一道具时，屏幕左下角显示操作提示，右下角显示目标坐标。

## 工作台整合

- 地图工作台提供“获得工具”，可领取地图规划器和地图画笔。
- 重复领取只补缺失工具；背包、副手及鼠标上已有的工具（包括改名工具）不会重复发放。背包满时会提示腾出空位。
- 地图编辑页提供“打开规划器”“打开画笔”和“获得工具”。
- 规划器菜单与画笔菜单都保留“地图工作台”入口，便于切换地图和分享。
- 编辑页的“脚下”按钮只修改草稿，需要点击“保存区域”；画笔完成框选会自动保存。

## r83 修复

- 修复拿地图工具左键点击空气时取消不可取消事件造成的客户端崩溃。
- 蹲下右键统一为打开画笔菜单，服务端不再暗中切换模式；编辑操作统一由画笔数据包执行。
- 教程文字自动换行并随菜单滚动，手持提示按当前目标与模式显示。

## r84 修复

- 修复地图同步包中 C/D 队人数和区域数据读写顺序不一致，导致区域、选中目标和距离无法正确同步的问题。
- 范围线框与填充分开绘制；第一角确认后显示黄色待完成范围，第二角完成后显示已保存区域。保存失败时保留角点并显示原因。
- 空气距离使用 1～64 格滑块和加减按钮；左键点击的实际方块不再受空气距离限制。
- 左键只处理按下事件，避免松开或持续挖掘重复提交角点。
- 联机协议升级为 13，客户端和服务器都需要更新到 r84。
