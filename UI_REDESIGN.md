# 团队死斗 r76：界面重设计与逐项功能建议

本轮修改针对 Minecraft 1.20.1 / Forge 47.4.0 的原生客户端界面。以下“已实现”描述 r76 的客户端改动；“建议”是后续规划，未实现的服务端能力不会用假数据展示。旧 HUD 配置、统计接口与网络协议继续沿用。

## 1. 统一界面体系

采用石墨灰工作区、绿色主操作、红蓝阵营色和黄色等待状态。窗口按标题、内容、反馈、操作四个区带布局。返回使用中性按钮；破坏性操作使用红色确认。列表为紧凑双行结构，选中、人数、权限与状态均可快速辨认。

| 功能 | r76 处理 | 后续建议 |
| --- | --- | --- |
| 导航与返回 | 入口收拢到团队死斗大厅；子窗口保留返回链 | 增加最近访问的房间与地图，但不要自动重新加入 |
| 列表 | 共用搜索、选中、滚轮、方向键与空结果状态 | 补充读屏播报、分页计数、排序和多条件过滤 |
| 按钮 | 主操作、普通操作、危险操作分色，完整含义保留在提示中 | 更多纯工具按钮采用图标，检查游戏 Unicode 字库 |
| 开关与选项 | 布尔值显示为开关；多选项使用下拉菜单，支持方向键、回车和 Esc | 数据源菜单增加分组搜索，避免大量选项需要长距离滚动 |
| 输入 | 输入框统一焦点、占位内容和密码遮罩；多处输入保留草稿 | 数字项增加步进器和字段级范围错误；同步所有界面文本到语言文件 |
| 状态反馈 | 固定消息区，列表不再被临时消息浮层遮住 | 统一请求编号、超时重试与成功确认，防止轮询消息覆盖操作结果 |
| 小窗口 | 流式内容滚动，固定底栏，超长文本按宽度省略 | 实机覆盖不同 GUI 缩放、资源包字体和高 DPI 环境 |
| 无障碍 | 保留原版控件焦点；列表和菜单可用键盘 | 增加高对比模式、色弱配色、读屏选中提示和完整键盘焦点审核 |

## 2. 团队死斗大厅

| 功能 | r76 处理 | 后续建议 |
| --- | --- | --- |
| 房间大厅 / 我的房间 | 根据玩家是否在房间中切换入口 | 显示最近房间，但重连与重新入房应独立确认 |
| 快速匹配 | 保留排队与已匹配状态，成局时仍可查看匹配页 | 允许预先选择模式偏好，服务器解释预计等待条件 |
| 地图工作台 | 加入大厅内的直接入口 | 普通玩家与管理员显示不同管理操作 |
| HUD 编辑器 | 加入大厅内的直接入口 | 给每个玩家保存多个配置预设 |
| 返回游戏 | 使用中性底部操作 | 暂停菜单中的原版入口按钮也可后续单独统一本 MOD 样式 |

## 3. 房间大厅与创建房间

| 功能 | r76 处理 | 后续建议 |
| --- | --- | --- |
| 浏览房间 | 名称、模式、房主、地图、人数字段分层；展示私密与比赛状态 | 增加只看可加入、模式、公开/私密过滤及人数排序 |
| 搜索房间 | 支持名称、模式、房主和 ID 的不区分大小写检索 | 服务端房间超过协议上限时改为分页检索 |
| 加入房间 | 检查开放状态、容量、已有房间和排队状态 | 显示具体禁用原因，支持密码错误后焦点回到密码框 |
| 房间密码 | 输入遮罩，保留返回后的草稿 | 加入密码与创建密码完全隔离，加入成功后清空 |
| 创建房间 | 新增独立窗口，集中填写名称、密码与人数 | 增加模式选择和常用规则预设，服务端返回最终采用的配置 |
| 人数上限 | 明示 2～64，0 使用服务端默认值 | 不应把 0 展示成不限；不限人数应是单独的服务器策略 |
| 创建请求 | 提交期间禁用重复创建，收到入房信息后进入房间 | 超时信息应区分网络延迟、房间数上限和权限问题 |
| 我的房间 | 独立快捷入口 | 展示所属房间名称和当前阶段 |
| 刷新 | 手动刷新及定期刷新共存 | 数据更新时维持选中项与滚动位置，并采用差量同步 |

## 4. 我的房间

| 功能 | r76 处理 | 后续建议 |
| --- | --- | --- |
| 房间概况 | 标题显示房间名与人数，正文保留规则摘要 | 独立展示公开/私密、房主、区域和服务器延迟 |
| 成员名单 | 可搜索、滚动查看全员，区分房主、成员和自己 | 扩展协议支持玩家 UUID、皮肤头像、队伍、准备状态和延迟 |
| 选择地图 | 保留房主权限与开赛锁定检查 | 展示地图缩略图、出生点完整性、快照状态和推荐人数 |
| 规则设置 | 所有人可查看，只有可编辑状态下的房主可修改 | 非房主显示改动摘要；房主可保存命名规则预设 |
| 开始比赛 | 明示缺少地图、人数不足、等待房主等条件 | 服务端返回一份完整的开赛检查结果，而非一次只显示一个问题 |
| 离开房间 | 增加确认，并说明比赛退出和房主转移 | 房主主动转移权限、成员踢出和解散房间需要独立服务端授权 |
| 状态刷新 | 保留自动同步；退出房间后返回上级 | 比赛启动失败时保留房间与选图，并提示可重试原因 |
| 准备与分队 | 本轮未添加假准备按钮或假队伍名单 | 优先实现准备/取消准备、红蓝阵营名单、观战席和人数平衡提示 |

## 5. 快速匹配

| 功能 | r76 处理 | 后续建议 |
| --- | --- | --- |
| 开始 / 取消 | 单一主按钮随队列状态变化 | 请求尚未确认时增加独立等待态和超时重试 |
| 队列人数与序位 | 直接显示服务器同步数据 | 明确序位的含义；不要将本服人数误称为全服匹配池 |
| 已等待时间 | 保留客户端平滑计时 | 有可靠历史数据后再展示预计等待时间 |
| 成局倒计时 | 强调匹配成功与入场阶段 | 增加可配置的成局提示音，并尊重音量设置 |
| 进度条 | 人数达到成局门槛 / 准备倒计时分别驱动 | 不是预计匹配完成百分比；未来可改为分段人数格 |
| 匹配规则 | 保留服务器规定的最低人数与准备秒数 | 规则应完全由协议同步，避免客户端常量与服务器策略不一致 |
| 返回 | 返回不取消排队；游戏内场景 HUD 继续显示 | 增加组队匹配、模式偏好、重连保护，需改动服务端匹配逻辑 |

## 6. 比赛地图选择

| 功能 | r76 处理 | 后续建议 |
| --- | --- | --- |
| 地图列表 | 名称、作者和来源分层展示，标识当前使用地图 | 添加实际地图缩略图、适用模式、建议人数、地图尺寸 |
| 搜索 | 搜索名称、作者与 ID | 增加收藏、最近使用和模式筛选 |
| 选用 | 校验房主和房间状态，等待同步的地图 ID 生效后返回 | 使用请求编号关联错误，显示快照准备进度与明确超时 |
| 刷新 | 保留手动和定期刷新 | 新增地图排序稳定，防止列表更新导致误选 |
| 无效地图 | 不虚构可用性数据 | 服务端返回出生点、维度、快照检查结果；不可开赛的图直接解释原因 |

## 7. 地图工作台与创建地图

| 功能 | r76 处理 | 后续建议 |
| --- | --- | --- |
| 我的 / 服务器地图 | 显示拥有者与 ID，管理员沿用既有授权 | 增加来源筛选、修改时间、草稿/已发布状态 |
| 新建地图 | 创建页显示名称、当前维度、位置和拥有者 | 名称实时校验；创建后跳到区域或出生点中尚未完成的步骤 |
| 创建结果 | 保留请求编号匹配；“去编辑”实际进入地图编辑器 | 对超时建立明确失败/重试流程，避免重复创建 |
| 编辑地图 | 设置服务器编辑目标后进入编辑器 | 进入前等待目标确认，避免网络延迟下短暂显示上一个目标 |
| 删除地图 | 显示目标名称并二次确认；锁定时禁用 | 增加可恢复归档与版本备份，删除前列出依赖该图的房间 |
| 生成邀请码 | 对所选地图操作 | 展示生成时间、有效期和使用次数 |
| 复制邀请码 | 新增直接复制按钮 | 增加格式化分享文本与二维码需单独设计，不泄露服务器私有信息 |
| 撤销邀请码 | 显示具体地图并确认 | 说明既有副本不受影响；服务器记录撤销审计 |
| 邀请码导入 | 保留六位字母数字限制，不完整时禁用 | 导入前显示来源地图、作者、版本及占用空间，再确认创建副本 |
| 刷新 | 定期读取服务器地图状态 | 对较大列表使用分页和变化通知 |

## 8. 地图编辑器

| 功能 | r76 处理 | 后续建议 |
| --- | --- | --- |
| 区域 / 出生点页签 | 操作移到摘要之前，窄窗口先看到工具 | 将当前页签固定在滚动区顶部 |
| 边界最小 / 最大点 | 保留使用玩家当前位置的操作 | 同时提供坐标输入、选点工具和明确的世界预览 |
| 重置区最小 / 最大点 | 保留草稿与正式区域区分 | 显示方块体积、越界错误和预计复位成本 |
| 保存区域 | 继续调用服务器区域保存流程 | 增加草稿脏标记、放弃草稿、保存前校验与版本冲突提示 |
| A / B 队出生点 | 保留添加与总数量显示 | 列表展示每个坐标、朝向、危险检查；支持逐个删除和测试传送 |
| 观战点 | 保留设置与计数 | 支持多个观战点、巡回顺序和安全高度校验 |
| 清空出生点 | 三种清空操作均增加确认，并核对目标地图未变 | 提供服务端撤销记录，优先使用逐项删除替代全清 |
| 快照状态 | 保留维度、区域和快照摘要 | 将捕获/复位任务进度、失败方块数、备份时间放在独立任务区 |
| 出生点完整度 | 用小型进度条反映 A/B/观战点是否齐备 | 完整度不能等同于地图验证通过；需要服务端检查碰撞与安全性 |
| 草稿失效 / 锁定 | 保留失效提示和刷新入口 | 展示锁定来源、任务进度；刷新前提示将丢弃的改动 |

## 9. 房间规则：全部字段

规则按“比赛 / 玩家 / 高级”分组，字段标签与控件上下排列；模式与最少开赛人数在各组保留。模式相关字段仍由既有规则逻辑决定是否显示。返回时确认未保存改动，窗口缩放时保留草稿。

| 字段 | 建议 |
| --- | --- |
| 游戏模式 | 使用本轮下拉菜单；切换模板前说明哪些参数会重置，后续增加可撤销切换 |
| 最少开赛人数 | 数字步进，显示与房间容量的冲突 |
| 击杀目标 | 将“无限制”作为独立选项，避免用户必须理解 0 的约定 |
| 回合 / 比赛时长 | 使用分秒输入，并显示当前模式的时间到判定方式 |
| 需要获胜的回合数 | 增加单局、BO3、BO5 预设，同时保留自定义 |
| 热身时长 | 允许 0，预览完整开赛时间轴 |
| 阵亡恢复延迟 | 显示秒单位，结合自动复活开关检查有效性 |
| 自动复活 | 仅在支持复活的模式显示；本轮使用开关 |
| 友军伤害 | 默认值服从服务器；开启时在房间摘要中明显标识 |
| 换边间隔 | 显示按出生区换边还是阵营身份换边，避免语义含糊 |
| 回合结算间隔 | 限制过短值，避免玩家尚未看到结果就进入下一回合 |
| 整场结算停留 | 与战绩页关闭行为协调；房主不应强制所有客户端同时关战绩页 |
| 死亡保留背包 | 说明与服务器背包规则的优先级，服务端统一执行 |
| 隐藏死亡消息 | 与击杀播报独立控制，避免两种提示重复 |
| 赛后自动复位 | 地图没有快照时显示原因，不静默忽略 |
| 要求双方人数达标 | 明示人数门槛，以及离队后如何处理 |
| 换队政策 | 下拉展示禁止、仅开赛前、随时；服务端验证实际请求 |
| 自动平衡 | 展示触发时机，避免玩家将其理解成持续强制换队 |
| 出生点策略 | 给随机、轮换、远离敌人各一个短提示；无有效点时报告问题 |
| 两队最大人数差 | 解释奇数人数下无法绝对均分；结合实际人数展示结果 |
| 保存规则 | 本轮将“已保存”的提前宣称改成“已提交”；后续用请求编号确认整个规则版本 |
| 恢复默认 | 显示将变化的字段，允许取消；本轮继续保持只改草稿、不自动保存 |
| 刷新 / 只读 | 区分刷新服务器状态与覆盖本地草稿；成员和开赛后保持只读 |

## 10. HUD 编辑器：全部功能组

| 功能 | r76 处理 | 后续建议 |
| --- | --- | --- |
| 场景页签 | 保留死斗、爆破、歼灭、匹配、房间、全局场景 | 增加预设复制到另一个场景，避免重复配置 |
| 组件 / 属性 | 分为两页，选中组件后进入属性页，常用属性优先 | 组件多时增加层级树、搜索、锁定和排序 |
| 画布与属性面板 | 保留拖动、缩放属性面板；新增折叠按钮查看完整画面 | 参考线吸附、网格间距、对齐分布、多选与组合 |
| 内置记分板 | 可见性、位置、宽度、缩放、透明度、颜色及五个文本模板保留 | 将队伍名与分数分为独立语义字段，支持色弱阵营符号 |
| 状态文字 | 可见性、位置、宽度、缩放、透明度、颜色与模板保留 | 阵亡倒计时、观战、地图复位、等待入场分别设置优先级 |
| 击杀播报 | 可见性、位置、缩放、透明度、颜色和模板保留 | 增加多条队列、武器图标、队伍色、数量上限和停留时间 |
| 场景横幅 | 匹配和房间的两行模板、颜色、位置和缩放保留 | 本轮固定基础宽度减少内容变化跳动；后续提供用户可调宽度 |
| 文字模块 | 固定文本、动态数据源、文本大小、位置、尺寸等保留 | 模板占位符插入菜单、即时错误提示和长文本换行策略 |
| 色块模块 | 颜色、位置、尺寸、透明度等保留 | 支持命名与锁定，避免拖动时误选背景块 |
| 进度条模块 | 保留统计数值/上限绑定及外观设置 | 明确无数据与零值区别，支持方向与分段样式 |
| 图片模块 | 保留本地 PNG 选择、尺寸和透明度 | 缩略图选择、保持长宽比、缺图占位和尺寸限制提示 |
| 添加 / 删除模块 | 保留四类添加与可撤销删除 | 增加复制模块、重命名与图层顺序 |
| 背景 / 边框 / 阴影 | 继续独立控制 | 同类设置集中为一组，预览保持与实战一致 |
| 颜色预设与 HSV | 色板限制色块尺寸，滚动裁切避免侵入底栏；修正滑杆命中换算 | 增加十六进制直接输入、最近使用颜色和取色器 |
| 数值滑条 | 修正左右/上下键方向与长标签溢出 | 每个滑条增加可直接输入的精确数值，并区分 1 与 10 的步进 |
| 数据源选择 | 沿用比赛、匹配、房间、玩家、自定义通道，改为下拉 | 分类搜索、实时样值、可用性和单位；自定义通道只显示实际注册项 |
| 背景图片 / 图片刷新 | 沿用本地缓存和目录 | 使用文件选择器、文件格式检查；图片加载失败直接在对应控件反馈 |
| 参考线 | 保留中心线、三分线与底部安全区 | 参考线只用于编辑，不进入比赛；增加热键提示到悬浮说明 |
| 紧凑 / 居中 / 宽屏预设 | 保留并移到属性之后 | 导入导出配置、命名预设、按显示器保存 |
| 撤销 / 重做 | 保留事务历史，按钮使用像素图标 | 显示可撤销操作名称；拖动与连续输入应各合并成一步 |
| 当前场景默认 / 全部默认 | 保留可撤销恢复 | 恢复全部前列出影响范围 |
| 保存 / 放弃 / 继续编辑 | 保留原有草稿保存逻辑；显示保存状态 | 保存失败时保留全部草稿，并给出可定位的文件错误 |

## 11. 战斗与场景 HUD

本轮将比分绘制提取为编辑器与实战共用的代码：A 队、计时器、B 队各占独立列，避免长队伍文本覆盖时间。状态条、播报和场景横幅使用统一绘制函数；长文本裁切，极端缩放按视口适配。新用户默认布局和三个布局预设将播报移到比分区下方；已有自定义布局保留，可使用“当前页默认”或布局预设采用新位置。折叠属性面板时同时隐藏顶部场景页签和参考线，保留右下角返回编辑按钮。

优先建议增加独立赛后战绩界面（胜负、回合比分、个人击杀/死亡/伤害、再来一局、返回大厅），以及明确的观战控制与复活倒计时。当前代码没有完整的这些独立窗口，本轮不展示假战绩、假准备状态或不存在的观战操作。

## 12. 实施优先级与验证范围

1. 优先：服务端请求确认、房间准备状态、地图开赛校验、逐个出生点管理、删除/覆盖操作的可恢复备份。
2. 随后：地图缩略图、模式筛选、完整成员队伍信息、赛后战绩、HUD 图层与配置导出。
3. 再后：组队匹配、规则预设市场、地图版本发布与邀请协作权限。

自动化检查覆盖 320×240、480×270、640×360、960×540 的 GUI 逻辑尺寸、可见控件边界和重叠、下拉菜单取消/提交、HUD 长文本边界。测试会在 `build/ui-previews/` 生成界面实际绘制调用的离屏诊断 PNG；它们使用替代字体和模拟数据，不是游戏 OpenGL 截图，不代表多人服务器交互已经实测通过。

发布前仍需真实客户端验证 GUI 缩放/资源包字体、房主与普通成员、公开与密码房、匹配成局/取消、地图创建/删除/分享/选图、三种模式的比赛结束，以及 HUD 保存后重启读取。

## 13. r78 内置 HUD 模板

入口：HUD 编辑器 → 选择场景 → 组件 → 内置 HUD 模板（位于组件列表下方，可能需要滚动）。

| 模板 | 风格 |
| --- | --- |
| 反恐精英 | 紧凑顶部比分，金色强调，左下轻量状态条，右侧击杀信息 |
| 穿越火线 | 宽幅顶部比分，青色强调，金色击杀信息，较高不透明度 |
| 使命召唤 | 左上比分，白色与绿色强调，左下状态条，低不透明度 |

这些是基于本 MOD 现有组件制作的风格化预设，不是原游戏 HUD 的逐像素复刻，不附带原游戏素材、雷达或额外武器数据。
应用立即进入预览，仅覆盖当前场景内置组件的位置、大小、颜色、透明度、文字模板和显示状态；保留自定义模块、背景及其他场景。可撤销，保存后持久化，之后仍可逐项编辑。
匹配与房间场景自动使用对应的真实状态占位符。三套模板覆盖五种场景的有效性与重复应用测试，并生成四种逻辑分辨率的比赛预览诊断图。

## 14. r79 分列房间与多队对抗

房主可在房间中选择 2、3、4 支独立队伍，默认仍为 A/B 两队；队数不能超过房间容量。
成员按 A（红）、B（蓝）、C（绿）、D（黄）分列显示，包含成员名、房主及本人标记。
宽屏显示全部队伍，小窗口使用左右翻页；成员可搜索，各列单独滚动。有观战成员时另显示只读观战列。

### 选队与开赛

- 在开放的自建房间中，点击队伍列顶部的“加入”切换自己的队伍，服务端确认后所有成员同步更新。
- 房主可调整队数；减少队数时，被移除队伍的成员重新分配到剩余人数较少的队伍，不会被踢出房间。
- 房间选队允许人数不均，但每支启用队伍至少一人，并达到房间最少开赛人数才能开赛。
- 开赛按房间选队入场，不再被开赛自动平衡重新分队。赛中换队仍受既有换队政策和人数差限制。
- 开赛倒计时、等待地图载入、比赛进行中禁止通过房间换队或修改队数。快速匹配继续使用双队自动分配，不开放手动选队。
- 服务端验证请求所属房间、玩家身份、房主权限、队伍范围和房间状态；无效请求不会修改队伍。

### 地图与胜负

- 地图编辑器新增 C/D 队出生点的添加、清空与数量显示。也支持管理员出生点命令的 c、d 参数。
- 地图 JSON 在既有 `spawns.teamA/teamB/spectator` 外增加 `teamC/teamD`；旧地图缺少这两项时按空列表读取。
- 第三、第四队必须有自己的出生点；缺失时提示具体队名并取消开赛，不会借用 A/B 出生点。
- 四队独立累计本轮得分、整场击杀、伤害和胜场。友军为同队玩家，其他启用队伍均为敌方。
- 死斗按目标击杀或超时最高得分判胜；歼灭类模式淘汰一队不会提前结束，剩最后一队时判胜。超时先比存活人数、再比击杀，并列则平局。
- 双队爆破保留原有换边；三队及以上禁用双队换边，使用各自独立出生区。
- HUD 多队比赛自动展示所有队伍的比分及胜场；编辑器根据当前房间预览。自定义统计新增 C/D 数据源，“敌方”统计在多队时取其他队伍的对应最高值。

### 升级与验证

r79 网络协议版本为 11，服务器和所有客户端必须同时升级，不能与 r78 混用。
自动测试覆盖房间队伍分配、换队与权限拒绝、减少队伍后的成员保留、网络往返与客户端镜像、C/D 出生点持久化、第四队获胜与平局、歼灭不提前结算、缺少出生点阻止开赛，以及 320×240 至 960×540 的界面诊断预览。
测试中的 Minecraft 注册表已初始化，但 Forge 启动器专属网络钩子使用模拟；这些测试不等于真实多人服务器联机验收。仍需实服验证多人换队、三/四队完整比赛、回合重置和重连。
