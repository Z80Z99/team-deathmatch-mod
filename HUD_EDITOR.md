# HUD 编辑器 r80

## 示例与删除

组件页中的内置组件是用文字、数值、色块、强调线等独立元素拼出来的示例，不是不可拆分的面板。

- 选中画面中的元素或组件列表中的条目，在属性页点“删除此元素”。背景和数值各有自己的条目。
- 删除、添加、套用模板都支持撤销和重做。清空后保存，重新进入不会恢复已删除元素。
- “添加组件示例”可以重新添加记分板、状态文字、击杀播报或场景横幅。
- 元素保留独立锚点和像素偏移；更换窗口尺寸时整组示例按同一参考尺寸适配，但不依赖父元素。
- 在属性页调节位置、偏移、尺寸、颜色、透明度，或直接拖动元素。

## 参数配置

文字和进度条属性中有“参数配置”小标题，旁边的 `?` 打开可搜索的说明书。
说明书列出当前注册的全部参数，包括第三方数据源的名称和示例值。

| 输入 | 示例输出 |
| --- | --- |
| `{round}` | `1` |
| `回合 {round}` | `回合 1` |
| `目标 {target}` | `目标 25` |
| `A队 {score_a}` | `A队 12` |
| `A队 114` | `A队 114`，不追加分数 |
| `延迟 {ping} ms` | `延迟 38 ms` |
| `已等待 {wait}` | `已等待 00:18` |
| `人数 {members}/{room_max}` | `人数 4/12` |

不需要绑定数据源就可以使用 `{参数名}`，一行可包含多个参数。未知参数原样保留。
标签、空格、标点和单位写在参数外；数值参数不自动加“回合”“目标”等文字。
`target` 在死斗中为目标击杀数，在回合模式中为所需胜场；0 表示按时间判定。

选择绑定数据源时也可以使用 `%s`（格式化值）、`%v`（原始数）、`%m`（上限）。
进度条的 `%s` 是百分比，空模板只显示进度条。固定文字不隐式追加数据源数值。

旧的整句来源（如 `matching_line1`、`room_line1`、`team_sizes_text`）仅作为兼容接口保留，说明书明确标记。
新的组件示例不再依赖这些整句拼装；数值别名 `round_text`、`target_text` 也改为只返回值。
状态消息本身是文本，例如匹配提示或当前房间状态，仍按原消息显示。

## 内置模板

三套模板现在位于“组件”页，不在“属性”页。

- CS2 风格：顶部紧凑计时与分队比分、分队人数、金色生命状态。
- 穿越火线风格：宽幅顶部比分、独立队伍色条、生命与护甲状态、击杀播报。
- 使命召唤20 / MWIII 风格：左上分队比分和进度、轻量计时与个人统计。

每套模板支持死斗、爆破、歼灭三个比赛场景，以及匹配和房间场景。死斗显示击杀分，回合模式显示胜场。
C、D 队元素只在相应队伍存在时显示；击杀播报在事件有效期内显示，不常驻假数据。
套用模板会替换当前场景的示例元素（含修改过的示例），保留用户自行添加的元素和全局背景，操作可撤销。

这些是基于 MOD 已有数据和原生元素的风格适配，不是原游戏 HUD 的完整复刻。
目前没有枪械弹药、雷达、技能和连杀奖励等统一数据接口，因此模板不伪造这些数值或功能。
原游戏视觉参考：[CS2 官方页面](https://www.counter-strike.net/cs2)、[MWIII 官方指南](https://www.callofduty.com/guides/modernwarfare3)。

## 旧配置

配置升级到版本 6。首次读取旧配置时尝试保留 `config/fpsmod/client-hud.before-v6.json` 备份，不覆盖已有备份。
旧记分板等组件转换为独立元素，旧隐式前缀转成显式模板；原有自定义元素保留。
编辑完成后仍使用“保存并关闭”写入配置；说明书返回不会丢失未保存的编辑。

构建测试包含参数、迁移、独立删除、删除后不复活、模板幂等性、用户元素保留、实战删除标记和多个分辨率布局。
`build/ui-previews` 下的图片是模拟绘制诊断图，不是实际 Minecraft OpenGL 客户端截图。
